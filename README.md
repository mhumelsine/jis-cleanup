# Bad Cases

## 2006CF2362

v_pnx2jis_bad_dkt -> should be all bad dockets

Would have to use regex to parse out.

__bad docket
__location or status change or not
__does not have user activity after the change



If all entries after 8/18 should be machine only.


PNXLOG 

Person was never booked into the jail

PNX2JIS -> why did it not touch the


AUDIT_TRAIL contains only JISJDW, SYSTEMA, (ssytem)
INMATE table shows INMATE was not booked in the jail during that time, we can assume there were no user actions in 
that time.
The create userID in INMATE will always be the user that created (no trigger)
CJIS_SPN

Look for location, bond amount changes, status in audit trail







